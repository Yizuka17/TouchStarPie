name: Build and Test

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    name: Build & Verify StarPie
    runs-on: windows-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup .NET SDK 8.0
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore Dependencies
      run: dotnet restore WinPieGestures/WinPieGestures.csproj -p:Platform=x64

    - name: Build Solution (Release)
      run: dotnet build WinPieGestures/WinPieGestures.csproj -c Release -p:Platform=x64 --no-restore

    - name: Publish Standalone Binary
      run: dotnet publish WinPieGestures/WinPieGestures.csproj -c Release -r win-x64 -p:Platform=x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o bin/Standalone

    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: StarPie-Standalone-win-x64
        path: bin/Standalone/StarPie.exe
